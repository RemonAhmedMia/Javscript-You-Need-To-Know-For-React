// **04 For Loop=====

const bondhu = ['Rimon', 'Shakib' , 'Akib'];
for (let index = 0; index < bondhu.length; index++) {
    const element = bondhu[index];
    console.log(element);
}

// **05=====Function====

function sum (a,b) {
    const result = a + b;
    return result;
}

const final = sum (4,6);
console.log(final);
